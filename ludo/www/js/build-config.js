// Overwritten by ludo/scripts/write-build-config.mjs during CI when a
// Firebase project is supplied. Committed empty so the generic build asks
// for a project on first launch instead of hard-coding anyone's.
export const BUILD_CONFIG = null;
