// PLACEHOLDER. The Android build overwrites this file with the real
// Capacitor plugin bundle (`npm run build:plugins`, see ../../../package.json).
//
// It is committed as a no-op so the web build — which has no native bridge
// and needs no plugins — loads a valid, empty file instead of 404ing on a
// script tag. Leaving window.CapPlugins undefined is exactly what js/app.js
// expects on the web: it guards every use with `window.CapPlugins?.App`.
